---
layout: ../layouts/Page.astro
isFrontPage: true
---

This is a demo and test site for the <a href="https://github.com/swup/fragment-plugin">fragment plugin</a> for <a href="https://github.com/swup/">swup</a>.

Under <a href="/characters/">Characters</a>, you can find a filterable list of super mario characters. Note how only the element <code>#characters-list</code> is replaced when filtering characters. How a single character opens in an <code>#character-modal</code>. How only the <code>#character-detail</code> of a character is being replaced between characters, without touching the <code>#character-modal</code>.

<strong>All this is is the work of the Fragment Plugin! 🪄</strong>