---
---

<script>
	import "../js/global.js";
</script>

<style is:inline>
	/*
	* Expressive Code Overwrites
	*/

	/* Copy Tooltip */
	.ec.ec-of75f .copy .feedback {
		--tooltip-bg: var(--swup-green);
		color: black;
	}
	/* markers */
	.ec-line mark::before {
		pointer-events: none;
	}
	/* Frame */
	.ec.ec-of75f .frame {
		border: none;
		box-shadow: none;
	}
	.ec.ec-of75f .frame pre {
		border-radius: var(--radius-1);
	}
	.frame:not(.is-terminal) pre {
		border-top-right-radius: var(--radius-1) !important;
	}
	/* Header */
	.ec.ec-of75f .frame .header {
		border: none;
	}
	.ec.ec-of75f .frame.has-title:not(.is-terminal) .header {
		background: transparent;
	}
	.ec.ec-of75f .frame.has-title:not(.is-terminal) .title {
		border: none;
		background: #282a36;
		color: var(--swup-green);
		border-bottom: 1px dotted rgb(255 255 255 / 0.3);
		font-size: 0.8rem;
	}
	.ec.ec-of75f .frame.has-title:not(.is-terminal) .header::before,
	.ec.ec-of75f .frame.has-title:not(.is-terminal) .header::after {
		border: none !important;
	}

</style>
